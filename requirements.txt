# -- my own helper library
pyutilities>=0.13.1

# -- network interaction
urllib3>=1.26.4
requests==2.25.1
responses>=0.13.3  # used for testing
beautifulsoup4==4.9.3
PyYAML>=5.4.1

# -- frameworks / ORM / DBMS
Django>=3.2.4
# in order to install sqlite3 on python on linux you need to install the following:
#   sudo apt install sqlite3 sqlitebrowser libsqlite3-dev
pysqlite3>=0.4.6

# -- nice text tables (reporting to console)
prettytable==2.1.0

# -- testing dependencies + virtual environment
mock==4.0.3
nose2==0.10.0
virtualenv>=20.4.7

# -- work with word/excel files (old + new formats)
xlrd==2.0.1  # xls format read
xlwt==1.3.0  # xls format write
openpyxl>=3.0.7  # all excel formats read/write

# -- system
twine==3.4.1  # publishing python packages
